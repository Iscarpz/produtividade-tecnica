import { attentionGroups, callAgeInDays, sortMyQueue, workAgeInDays, type QueueOrder } from "@/lib/callPriorities";
import { AlertTriangle, ChevronRight, Clock3, Inbox, ShieldCheck, Wrench } from "lucide-react";
import React, { useMemo, useState } from "react";

function CallRow({ call, onSelect, accent = "bg-[#2E7D32]" }: { call: any; onSelect: (id: number) => void; accent?: string }) {
  const receiptDays = callAgeInDays(call); const progressDays = workAgeInDays(call);
  return <button onClick={() => onSelect(call.id)} className="flex w-full min-w-0 items-center gap-3 px-5 py-3.5 text-left transition hover:bg-[#E5E7EB]/45"><span className={`h-2.5 w-2.5 shrink-0 rounded-full ${accent}`}/><div className="min-w-0 flex-1"><div className="flex min-w-0 items-center gap-2"><span className="font-mono text-sm font-bold text-[#0D1117]">{call.numeroOs}</span><span className="truncate text-sm text-[#1A1F24]">{call.modelo}</span></div><p className="mt-1 truncate text-xs text-slate-500">Serial {call.serial} · {call.queixa}</p></div><div className="shrink-0 text-right text-[11px]"><p className="font-semibold text-[#1A1F24]">Receb. {receiptDays}d</p><p className="mt-1 text-slate-500">And. {progressDays === null ? "—" : `${progressDays}d`}</p></div><ChevronRight className="h-4 w-4 shrink-0 text-slate-400"/></button>;
}

function formatReceiptDate(value: Date | string | null | undefined) {
  if (!value) return "Não informado";
  return new Date(value).toLocaleDateString("pt-BR");
}

function ReceivedCard({ call, onStart, isStarting }: { call: any; onStart?: (id: number) => void; isStarting: boolean }) {
  return <article className="flex min-w-0 flex-col rounded-xl border border-[#E5E7EB] bg-white p-5 shadow-[0_1px_2px_rgba(13,17,23,.05)]"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-slate-500">Chamado recebido</p><p className="mt-1 font-mono text-sm font-bold text-[#0D1117]">{call.numeroOs}</p></div><span className="rounded-full bg-[#E5E7EB] px-2 py-1 text-[10px] font-semibold text-[#1A1F24]">RECEBIDO</span></div><p className="mt-3 truncate text-sm font-semibold text-[#1A1F24]">{call.modelo}</p><div className="mt-3 min-h-11 border-l-2 border-[#A3E635] pl-3"><p className="text-[10px] font-semibold uppercase tracking-[0.12em] text-slate-500">Queixa</p><p className="mt-1 line-clamp-2 text-xs leading-5 text-slate-600">{call.queixa || "Sem queixa informada"}</p></div><p className="mt-4 text-xs text-slate-500"><span className="font-semibold text-[#1A1F24]">Recebido:</span> {formatReceiptDate(call.dataEntrada)}</p>{onStart && <button type="button" disabled={isStarting} onClick={() => onStart(call.id)} className="mt-4 w-full rounded-md bg-[#2E7D32] px-3 py-2.5 text-xs font-semibold text-white transition hover:bg-[#0D1117] disabled:cursor-wait disabled:opacity-60">{isStarting ? "Colocando em andamento..." : "Colocar em andamento"}</button>}</article>;
}

function AttentionGroup({ title, calls, onSelect, icon: Icon }: { title: string; calls: any[]; onSelect: (id: number) => void; accent: string; icon: typeof AlertTriangle }) {
  if (!calls.length) return null;
  return <section className="border-t border-[#E5E7EB]"><div className="flex items-center gap-2 px-1 py-3"><Icon className="h-4 w-4 text-[#F5B700]"/><div><h3 className="text-sm font-semibold text-[#1A1F24]">{title}</h3><p className="text-[11px] text-slate-500">{calls.length} chamado{calls.length === 1 ? "" : "s"}</p></div></div><div className="divide-y divide-[#E5E7EB] bg-white">{calls.map((call) => <CallRow key={call.id} call={call} onSelect={onSelect} accent="bg-[#F5B700]"/>)}</div></section>;
}

export function OperationalOverview({ calls, onSelect, onStart, startingCallId }: { calls: any[]; onSelect: (id: number) => void; onStart?: (id: number) => void; startingCallId?: number | null }) {
  const [order, setOrder] = useState<QueueOrder>("oldest");
  const mine = useMemo(() => sortMyQueue(calls, order), [calls, order]);
  const attention = useMemo(() => attentionGroups(calls), [calls]);
  const received = useMemo(() => calls.filter((call) => call.status === "RECEBIDO"), [calls]);
  const totalAttention = attention.zurich.length + attention.nearTen.length + attention.nearThirty.length;
  return <><section className="mt-8 overflow-hidden rounded-xl border border-[#E5E7EB] bg-white shadow-[0_1px_2px_rgba(13,17,23,.05)]"><div className="flex flex-col gap-3 border-b border-[#E5E7EB] px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-lg bg-[#E5E7EB] text-[#2E7D32]"><Inbox className="h-5 w-5"/></span><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#2E7D32]">Chamados recebidos</p><p className="mt-1 text-sm text-slate-500">Chamados que chegaram ao setor e aguardam entrada na bancada.</p></div></div><span className="self-start rounded-full bg-[#E5E7EB] px-3 py-1.5 text-xs font-semibold text-[#1A1F24] sm:self-auto">{received.length} recebido{received.length === 1 ? "" : "s"}</span></div>{received.length ? <div className="grid gap-3 p-4 sm:grid-cols-2 xl:grid-cols-3">{received.map((call) => <ReceivedCard key={call.id} call={call} onStart={onStart} isStarting={startingCallId === call.id}/>)}</div> : <div className="p-8 text-center"><span className="tecbase-empty-icon"><Inbox className="h-5 w-5"/></span><p className="mt-3 text-sm font-medium text-[#1A1F24]">Nenhum chamado recebido aguardando bancada</p><p className="mt-1 text-xs text-slate-500">Os próximos recebimentos aparecerão aqui automaticamente.</p></div>}</section><div className="mt-7 grid gap-7 xl:grid-cols-[minmax(0,1.25fr)_minmax(0,1fr)]"><section className="overflow-hidden rounded-xl border border-[#E5E7EB] bg-white shadow-[0_1px_2px_rgba(13,17,23,.05)]"><div className="flex flex-col gap-4 border-b border-[#E5E7EB] p-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#2E7D32]">Minha fila</p><div className="mt-1 flex flex-wrap items-center gap-3"><h2 className="flex items-center gap-2 text-2xl font-bold text-[#0D1117]"><Wrench className="h-5 w-5 text-[#2E7D32]"/>Em andamento</h2><span className="rounded-full border border-[#E5E7EB] bg-white px-2.5 py-1 text-xs font-semibold text-[#2E7D32]">{mine.length} chamado{mine.length === 1 ? "" : "s"} em produção</span></div><p className="mt-1 text-sm text-slate-500">Priorize o próximo chamado sem alterar o status.</p></div><div className="grid w-full max-w-xs grid-cols-3 gap-1 rounded-lg bg-[#E5E7EB] p-1 sm:w-auto">{([ ["all", "Todos"], ["oldest", "Mais antigos"], ["newest", "Mais recentes"] ] as const).map(([value, label]) => <button key={value} onClick={() => setOrder(value)} className={`rounded-md px-2 py-2 text-[11px] font-semibold transition ${order === value ? "bg-white text-[#0D1117] shadow-sm" : "text-slate-500 hover:text-[#1A1F24]"}`}>{label}</button>)}</div></div><div className="divide-y divide-[#E5E7EB]">{mine.length ? mine.map((call) => <CallRow key={call.id} call={call} onSelect={onSelect}/>) : <div className="p-12 text-center"><span className="tecbase-empty-icon"><Wrench className="h-5 w-5"/></span><p className="mt-3 text-sm font-medium text-[#1A1F24]">Nenhum chamado em andamento</p><p className="mt-1 text-xs text-slate-500">Os chamados retornados das filas aparecerão aqui automaticamente.</p></div>}</div></section><section><div className="mb-4 flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[0.18em] text-[#2E7D32]">Atenção necessária</p><h2 className="mt-1 flex items-center gap-2 text-2xl font-bold text-[#0D1117]"><AlertTriangle className="h-5 w-5 text-[#F5B700]"/>Prioridades da operação</h2></div>{totalAttention > 0 && <span className="rounded-full bg-[#F5B700] px-3 py-1.5 text-xs font-semibold text-[#0D1117]">{totalAttention} em atenção</span>}</div><div className="space-y-4">{totalAttention ? <><AttentionGroup title="Zurich" calls={attention.zurich} onSelect={onSelect} accent="bg-[#F5B700]" icon={ShieldCheck}/><AttentionGroup title="Próximos de 10 dias" calls={attention.nearTen} onSelect={onSelect} accent="bg-[#F5B700]" icon={Clock3}/><AttentionGroup title="Próximos de 30 dias" calls={attention.nearThirty} onSelect={onSelect} accent="bg-[#F5B700]" icon={Clock3}/></> : <div className="rounded-xl border border-dashed border-[#E5E7EB] bg-white p-10 text-center"><span className="tecbase-empty-icon"><AlertTriangle className="h-5 w-5"/></span><p className="mt-3 text-sm font-medium text-[#1A1F24]">Nenhuma prioridade no momento</p><p className="mt-1 text-xs text-slate-500">A operação está dentro dos prazos de recebimento configurados.</p></div>}</div></section></div></>;
}
